# Password Reset Rate Limit

This is the flagship Sprigcode demo for the first GitHub push.

It shows a narrow but realistic change:

- resolve a specific call anchor
- add an import
- insert one statement before one call
- verify `match_count`
- verify `idempotent`

This is the smallest demo that still shows why typed semantic transactions are
safer than asking a tool to rewrite a file directly.

